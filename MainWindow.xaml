<Window x:Class="FawkesWeb.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FawkesWeb"
        mc:Ignorable="d"
        Title="FakewsWeb"
        Height="720"
        Width="1200"
        MinHeight="540"
        MinWidth="960"
        WindowStyle="None"
        Background="#0f172a"
        Foreground="#e5e7eb"
        ResizeMode="CanResize"
        Loaded="Window_Loaded"
        Activated="Window_Activated"
        Deactivated="Window_Deactivated">
    <Window.Resources>
        <Thickness x:Key="PagePadding">16</Thickness>
        <SolidColorBrush x:Key="AccentBrush" Color="#6366f1" />
        <SolidColorBrush x:Key="SurfaceBrush" Color="#111827" />
        <SolidColorBrush x:Key="SurfaceAltBrush" Color="#1f2937" />
        <Style TargetType="Button" x:Key="ToolbarButton">
            <Setter Property="Margin" Value="6,0,0,0" />
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Background" Value="{StaticResource SurfaceAltBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}" />
            <Setter Property="BorderBrush" Value="#2d3748" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#374151" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#4b5563" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Padding" Value="10,6" />
            <Setter Property="Background" Value="{StaticResource SurfaceAltBrush}" />
            <Setter Property="BorderBrush" Value="#1f2937" />
            <Setter Property="Foreground" Value="#e5e7eb" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="0,0,0,2" Padding="8,4">
                            <ContentPresenter ContentSource="Header" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                                <Setter TargetName="Bd" Property="Background" Value="#111827" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#1f2937" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36" />
            <RowDefinition Height="52" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Background="{StaticResource SurfaceAltBrush}" Grid.Row="0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" VerticalAlignment="Center" Margin="12,0,0,0">
                    <TextBlock Text="FakewsWeb" FontWeight="Bold" FontSize="14" VerticalAlignment="Center" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center" Margin="0,0,6,0">
                    <Button x:Name="MinButton" Content="—" Width="42" Click="Minimize_Click" Style="{StaticResource ToolbarButton}" Padding="0" />
                    <Button x:Name="MaxButton" Content="☐" Width="42" Click="MaximizeRestore_Click" Style="{StaticResource ToolbarButton}" Padding="0" />
                    <Button x:Name="CloseButton" Content="✕" Width="42" Click="Close_Click" Style="{StaticResource ToolbarButton}" Padding="0" Background="#b91c1c" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBox x:Name="AddressBar" Width="520" Height="32" Padding="10,6" Background="#0b1221" Foreground="#e5e7eb" BorderBrush="#1f2937" BorderThickness="1" Text="about:start" />
                    <Button Content="Go" Style="{StaticResource ToolbarButton}" Click="Navigate_Click" />
                    <Button Content="Allow cookies for this domain" Style="{StaticResource ToolbarButton}" Click="AllowCookies_Click" />
                    <Button x:Name="TorButton" Content="Connect to TOR network" Style="{StaticResource ToolbarButton}" Click="TorConnect_Click" />
                    <Button Content="AI summarize current tab" Style="{StaticResource ToolbarButton}" Click="SummarizeCurrent_Click" />
                    <Button Content="AI summarize all tabs" Style="{StaticResource ToolbarButton}" Click="SummarizeAll_Click" />
                    <Button Content="Speak website" Style="{StaticResource ToolbarButton}" Click="Speak_Click" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="Settings" Style="{StaticResource ToolbarButton}" Click="Settings_Click" />
                    <Button Content="New Tab" Style="{StaticResource ToolbarButton}" Click="NewTab_Click" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="2" Background="{StaticResource SurfaceAltBrush}">
            <TabControl x:Name="BrowserTabs" Background="{StaticResource SurfaceAltBrush}" BorderThickness="0" />
        </Grid>
    </Grid>
</Window>
